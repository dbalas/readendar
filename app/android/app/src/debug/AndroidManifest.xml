<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- The INTERNET permission is required for development. Specifically,
         the Flutter tool needs it to communicate with the running application
         to allow setting breakpoints, to provide hot reload, etc.
    -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- Debug-only: the native home-screen widget fetches via Kotlin
         HttpURLConnection, which (unlike Dart's networking) is subject to
         Android's cleartext policy. Allow cleartext so the widget can reach the
         local dev API over http://10.0.2.2. Production uses https, so this stays
         debug-only and never ships. -->
    <application android:usesCleartextTraffic="true" />
</manifest>
