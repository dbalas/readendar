<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Match the native window and system bars to Android dark mode before
         Flutter renders its first frame. -->
    <style name="LaunchTheme.Base" parent="@android:style/Theme.Material.NoActionBar">
        <!-- Show a splash screen on the activity. Automatically removed when
             the Flutter engine draws its first frame -->
        <item name="android:windowBackground">@drawable/launch_background</item>
        <item name="android:navigationBarColor">#FF0E1018</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
    <style name="LaunchTheme" parent="LaunchTheme.Base" />
    <!-- Theme applied to the Android Window as soon as the process has started.
         This theme determines the color of the Android Window while your
         Flutter UI initializes, as well as behind your Flutter UI while its
         running.

         This Theme is only used starting with V2 of Flutter's Android embedding. -->
    <style name="NormalTheme.Base" parent="@android:style/Theme.Material.NoActionBar">
        <item name="android:windowBackground">#FF0E1018</item>
        <item name="android:navigationBarColor">#FF0E1018</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
    <style name="NormalTheme" parent="NormalTheme.Base" />
</resources>
